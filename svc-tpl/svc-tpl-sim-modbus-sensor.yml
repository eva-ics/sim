command: /opt/sim/target/debug/sim-modbus-sensor
bus:
  path: var/bus.ipc
config:
  port_svc: sim.modbus1.port
  # Modbus unit ID
  unit: 2
  # INT, UINT, DINT, UDINT, LINT, ULINT, REAL or REALB (IEEE-754 big-endian)
  type: UINT
  # h for h@0 (holding), i for i@0 (input)
  reg: h
user: nobody
